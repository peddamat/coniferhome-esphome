wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  domain: .iot.home
  fast_connect: true
  ap:
    ssid: ${device_name}
    password: !secret esphome_ap_password
    ap_timeout: 30min
